#include "camera_runtime.hpp"

// CameraRuntime is intentionally implemented by the inline, value-oriented
// update_camera contract. Keep a translation unit so a future backend can
// attach platform-specific camera services without changing the raw model.
