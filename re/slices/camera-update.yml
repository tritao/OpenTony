version: 1
id: camera-update
subsystem: camera
status: active

scope:
  functions:
    - 0x0040f850
  types:
    - CCamera
  globals: []

artifacts:
  evidence:
    - re/evidence/functions/camera-system.md
  native:
    - src/camera/camera_math.hpp
    - src/camera/camera_update.hpp
    - src/camera/camera_system.hpp
  tests:
    - tests/test_camera_update.py
    - tests/test_camera_system.py

open_questions:
  - Capture live mode-25 scalar/basis producer values; the camera-side arithmetic, helper ordering, and probe inputs are now wired.
  - Separate the update mode and state branches without guessing semantic names.
  - Establish the target, anchor, smoothing, and viewport relationships.
  - Determine which recovered behavior belongs in the existing native camera math boundary.

completion:
  - The Camera_Update boundary, ABI, callers, and callees are evidence-backed.
  - Every accessed CCamera field is recorded with conservative confidence.
  - Important update modes and state transitions have a semantic model and tests.
  - Native camera behavior has an explicit progress mapping or a documented exclusion.
  - Exact matching work remains byte-identical where pursued.
